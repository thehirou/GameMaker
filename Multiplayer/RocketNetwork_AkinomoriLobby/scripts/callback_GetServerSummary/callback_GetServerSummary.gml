
function callback_GetServerSummary(collectionsStruct){
	
	//The collectionsStruct is a struct with keys as the names of all your collections
	// and values as an array of all doc names in that collection
	show_message(collectionsStruct)

}